@model IncomeModel

@{
    ViewData["Title"] = "Income";
}

<div>
    @Html.DisplayTextFor(m => m.DayJob.Description)
</div>
<div>
   @Model.DayJob.Amount.ToString("C")<br />
   @Model.PartTimeJob.Amount.ToString("C")<br />
   @Model.DoorDash.Amount.ToString("C")
</div>
